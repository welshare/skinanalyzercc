# Core dependencies
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
Pillow>=10.0.0

# Deep learning (ONNX runtime only - lightweight)
onnxruntime>=1.16.0

# API framework (optional, for serving)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Utilities
requests>=2.31.0
tqdm>=4.66.0

# Optional: PyTorch for BiSeNet model (CPU-only, much smaller)
# Install separately if needed: pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
# Or use: pip install -r requirements-optional.txt
